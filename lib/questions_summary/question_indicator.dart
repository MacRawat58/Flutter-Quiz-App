import 'package:flutter/material.dart';

class QuestionsIndicator extends StatelessWidget {
  const QuestionsIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
